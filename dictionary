from collections import OrderedDict

inventory = OrderedDict()

for _ in range(int(input())):
   
    name, price = input().rsplit(' ', 1)
    price = int(price)
    
   
    inventory[name] = inventory.get(name, 0) + price

for item, total in inventory.items():
    print(item, total)
